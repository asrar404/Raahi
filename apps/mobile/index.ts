import { registerRootComponent } from 'expo';

import App from './src/App';

// Registers App as the root component, and sets up the environment
// appropriately for both Expo Go and a standalone build.
registerRootComponent(App);
